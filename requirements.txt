# Бот УВД — зависимости (рекомендуется Python 3.11+)
# Важно: на Windows для Python 3.14 aiohttp может собираться из исходников — нужны
# Microsoft C++ Build Tools. Проще использовать Python 3.11 или 3.12 (готовые wheel'ы).
# Установка: pip install -r requirements.txt

# Основные зависимости
discord.py==2.3.2
python-dotenv==1.0.0
aiohttp==3.9.1

# База данных (async SQLite)
aiosqlite==0.19.0

# Разработка и CI (pytest, линтеры)
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
isort>=5.12.0
ruff>=0.1.0
